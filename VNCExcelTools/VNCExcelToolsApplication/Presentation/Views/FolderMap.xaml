<vncmvvm:ViewBase
    x:Class="VNCExcelToolsApplication.Presentation.Views.FolderMap"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"

    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:i1="http://schemas.microsoft.com/xaml/behaviors"
    
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" 
    xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"     
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    xmlns:dxdb="http://schemas.devexpress.com/winfx/2008/xaml/demobase"
    xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"  
    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
    xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
    xmlns:dxre="http://schemas.devexpress.com/winfx/2008/xaml/richedit"

    xmlns:views="clr-namespace:VNCExcelToolsApplication.Presentation.Views"
    xmlns:viewModels="clr-namespace:VNCExcelToolsApplication.Presentation.ViewModels"

    xmlns:core="clr-namespace:VNCExcelToolsApplication.Core;assembly=VNCExcelToolsApplication.Core"

    xmlns:vnccore="clr-namespace:VNC.Core;assembly=VNC.Core"
    xmlns:vncenum="clr-namespace:VNC.Core.Xaml.Enums;assembly=VNC.Core"
    xmlns:vncmvvm="clr-namespace:VNC.Core.Mvvm;assembly=VNC.Core"
    xmlns:vncviews="clr-namespace:VNC.WPF.Presentation.Views;assembly=VNC.WPF.Presentation"
    xmlns:vncconverters="clr-namespace:VNC.WPF.Presentation.Converters;assembly=VNC.WPF.Presentation"

    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="False"

    Background="{StaticResource FolderMapBackground}"

    d:DesignWidth="600" d:DesignHeight="150"
    d:DesignStyle="{StaticResource vncDesignStyleUC}"

    SizeChanged="thisControl_SizeChanged"
>
    <!--xmlns:converters="clr-namespace:VNCExcelTools.Presentation.Converters"-->

    <!-- Move this to Resources\Xaml\Display_StylesAndTemplates.xaml -->
     <!--<SolidColorBrush x:Key="FolderMapBackground" Color="#00FF00" />--> 

     <!--<d:DesignerProperties.DesignStyle> 
         <Style TargetType="UserControl"> 
             <Setter Property="Background" Value="White" /> 
         </Style> 
     </d:DesignerProperties.DesignStyle>--> 

    <!-- <vncmvvm:ViewBase.Resources> -->
        <!-- <localc:SelectedItemsToListOfStringConverter x:Key="SelectedItemsToListOfStringConverter" /> -->
        <!-- <localc:SelectedItemsConverter x:Key="SelectedItemsConverter" /> -->
        <!-- <localc:SelectedItemsToWorkItemQueryWrapperConverter x:Key="SelectedItemsToFolderMapWrapperConverter" /> -->
        <!-- <DataTemplate x:Key="dataTemplateFolderMap"> -->
            <!-- <StackPanel> -->
                <!-- <Label Content="{Binding XPath=@Name}" /> -->
            <!-- </StackPanel> -->
        <!-- </DataTemplate> -->

        <!-- <DataTemplate x:Key="dataTemplateFolderMapX"> -->
            <!-- <StackPanel> -->
                <!-- <Label Content="{Binding Name}" /> -->
            <!-- </StackPanel> -->
        <!-- </DataTemplate> -->
    <!-- </vnccore:ViewBase.Resources> -->

    <!-- Can declare ViewModel in Xaml -->
    <!-- <UserControl.DataContext> -->
        <!-- <localvm:FolderMapViewModel /> -->
    <!-- </UserControl.DataContext> -->

    <!-- Simple Layout using Grid -->

    <DockPanel Margin="5">

        <vncviews:DeveloperUIInfo x:Name="spDeveloperInfo" DockPanel.Dock="Bottom"/>

        <DockPanel VerticalAlignment="Top">

            <dxlc:LayoutGroup View="Group" Orientation="Vertical" DockPanel.Dock="Left" >
                <dxlc:LayoutItem>
                    <!-- Second form uses CommandParameter -->
                    <Button 
            		Command="{Binding CreateFolderMapCommand }"
            		Content="{Binding CreateFolderMapContent, FallbackValue='CreateFolderMap' }"
            		ToolTip="{Binding CreateFolderMapToolTip, FallbackValue='CreateFolderMap ToolTip' }" />
                    <!-- <dxlc:LayoutItem> -->
                    <!-- <Button -->
                    <!-- Command="{Binding CreateFolderMapCommand}" -->
                    <!-- CommandParameter="{Binding CreateFolderMapCommandParameter}" -->
                    <!-- Content="{Binding CreateFolderMapContent, FallbackValue='CreateFolderMap' }" -->
                    <!-- ToolTip="{Binding CreateFolderMapToolTip, FallbackValue='CreateFolderMap ToolTip' }" /> -->
                    <!-- <Button.Background> -->
                    <!-- <ImageBrush ImageSource="\Resources\Images\VNCDeveloperMotivation.png"  /> -->
                    <!-- </Button.Background> -->
                    <!-- </dxlc:LayoutItem> -->
                </dxlc:LayoutItem>
                <dxlc:LayoutGroup View="Group" Orientation="Horizontal" >
                    <!-- Second form uses CommandParameter -->
                    <dxlc:LayoutItem>
                        <Button 
                		Command="{Binding GroupDownCommand }"
                		Content="{Binding GroupDownContent, FallbackValue='GroupDown' }"
                		ToolTip="{Binding GroupDownToolTip, FallbackValue='GroupDown ToolTip' }" />
                    </dxlc:LayoutItem>
                    <!-- <dxlc:LayoutItem> -->
                    <!-- <Button -->
                    <!-- Command="{Binding GroupDownCommand}" -->
                    <!-- CommandParameter="{Binding GroupDownCommandParameter}" -->
                    <!-- Content="{Binding GroupDownContent, FallbackValue='GroupDown' }" -->
                    <!-- ToolTip="{Binding GroupDownToolTip, FallbackValue='GroupDown ToolTip' }" /> -->
                    <!-- <Button.Background> -->
                    <!-- <ImageBrush ImageSource="\Resources\Images\VNCDeveloperMotivation.png"  /> -->
                    <!-- </Button.Background> -->
                    <!-- </dxlc:LayoutItem> -->
                    
                    <!-- Second form uses CommandParameter -->
                    <dxlc:LayoutItem>
                        <Button 
                    		Command="{Binding GroupDownAllCommand }"
                    		Content="{Binding GroupDownAllContent, FallbackValue='GroupDownAll' }"
                    		ToolTip="{Binding GroupDownAllToolTip, FallbackValue='GroupDownAll ToolTip' }" />
                    </dxlc:LayoutItem>
                    <!-- <dxlc:LayoutItem> -->
                        <!-- <Button -->
                            <!-- Command="{Binding GroupDownAllCommand}" -->
                            <!-- CommandParameter="{Binding GroupDownAllCommandParameter}" -->
                            <!-- Content="{Binding GroupDownAllContent, FallbackValue='GroupDownAll' }" -->
                            <!-- ToolTip="{Binding GroupDownAllToolTip, FallbackValue='GroupDownAll ToolTip' }" /> -->
                            <!-- <Button.Background> -->
                                <!-- <ImageBrush ImageSource="\Resources\Images\VNCDeveloperMotivation.png"  /> -->
                            <!-- </Button.Background> -->
                    <!-- </dxlc:LayoutItem> -->
                    
                    <!-- Second form uses CommandParameter -->
                    <dxlc:LayoutItem>
                        <Button 
                    		Command="{Binding UngroupSelectionCommand }"
                    		Content="{Binding UngroupSelectionContent, FallbackValue='UngroupSelection' }"
                    		ToolTip="{Binding UngroupSelectionToolTip, FallbackValue='UngroupSelection ToolTip' }" />
                    </dxlc:LayoutItem>
                    <!-- <dxlc:LayoutItem> -->
                        <!-- <Button -->
                            <!-- Command="{Binding UngroupSelectionCommand}" -->
                            <!-- CommandParameter="{Binding UngroupSelectionCommandParameter}" -->
                            <!-- Content="{Binding UngroupSelectionContent, FallbackValue='UngroupSelection' }" -->
                            <!-- ToolTip="{Binding UngroupSelectionToolTip, FallbackValue='UngroupSelection ToolTip' }" /> -->
                            <!-- <Button.Background> -->
                                <!-- <ImageBrush ImageSource="\Resources\Images\VNCDeveloperMotivation.png"  /> -->
                            <!-- </Button.Background> -->
                    <!-- </dxlc:LayoutItem> -->
                    

         
                                        


                </dxlc:LayoutGroup>

            </dxlc:LayoutGroup>
            
            <dxlc:LayoutGroup View="Group" Orientation="Vertical" >
                
                <DockPanel>
                    <dxlc:LayoutItem DockPanel.Dock="Top" HorizontalAlignment="Center">
                        <Button 
                    		Command="{Binding SearchUpCommand }"
                    		Content="{Binding SearchUpContent, FallbackValue='SearchUp' }"
                    		ToolTip="{Binding SearchUpToolTip, FallbackValue='SearchUp ToolTip' }" />
                    </dxlc:LayoutItem>
                    <!-- <dxlc:LayoutItem> -->
                    <!-- <Button -->
                    <!-- Command="{Binding SearchUpCommand}" -->
                    <!-- CommandParameter="{Binding SearchUpCommandParameter}" -->
                    <!-- Content="{Binding SearchUpContent, FallbackValue='SearchUp' }" -->
                    <!-- ToolTip="{Binding SearchUpToolTip, FallbackValue='SearchUp ToolTip' }" /> -->
                    <!-- <Button.Background> -->
                    <!-- <ImageBrush ImageSource="\Resources\Images\VNCDeveloperMotivation.png"  /> -->
                    <!-- </Button.Background> -->
                    <!-- </dxlc:LayoutItem> -->
                    <!-- Second form uses CommandParameter -->
                    <dxlc:LayoutItem DockPanel.Dock="Bottom" HorizontalAlignment="Center">
                        <Button 
                    		Command="{Binding SearchDownCommand }"
                    		Content="{Binding SearchDownContent, FallbackValue='SearchDown' }"
                    		ToolTip="{Binding SearchDownToolTip, FallbackValue='SearchDown ToolTip' }" />
                    </dxlc:LayoutItem>
                    <!-- <dxlc:LayoutItem> -->
                    <!-- <Button -->
                    <!-- Command="{Binding SearchDownCommand}" -->
                    <!-- CommandParameter="{Binding SearchDownCommandParameter}" -->
                    <!-- Content="{Binding SearchDownContent, FallbackValue='SearchDown' }" -->
                    <!-- ToolTip="{Binding SearchDownToolTip, FallbackValue='SearchDown ToolTip' }" /> -->
                    <!-- <Button.Background> -->
                    <!-- <ImageBrush ImageSource="\Resources\Images\VNCDeveloperMotivation.png"  /> -->
                    <!-- </Button.Background> -->
                    <!-- </dxlc:LayoutItem> -->
                    <!-- Second form uses CommandParameter -->
                    <dxlc:LayoutItem DockPanel.Dock="Left" HorizontalAlignment="Left">
                        <Button 
                    		Command="{Binding SearchLeftCommand }"
                    		Content="{Binding SearchLeftContent, FallbackValue='SearchLeft' }"
                    		ToolTip="{Binding SearchLeftToolTip, FallbackValue='SearchLeft ToolTip' }" />
                    </dxlc:LayoutItem>
                    <!-- <dxlc:LayoutItem> -->
                        <!-- <Button -->
                            <!-- Command="{Binding SearchLeftCommand}" -->
                            <!-- CommandParameter="{Binding SearchLeftCommandParameter}" -->
                            <!-- Content="{Binding SearchLeftContent, FallbackValue='SearchLeft' }" -->
                            <!-- ToolTip="{Binding SearchLeftToolTip, FallbackValue='SearchLeft ToolTip' }" /> -->
                            <!-- <Button.Background> -->
                                <!-- <ImageBrush ImageSource="\Resources\Images\VNCDeveloperMotivation.png"  /> -->
                            <!-- </Button.Background> -->
                    <!-- </dxlc:LayoutItem> -->
                    <!-- Second form uses CommandParameter -->
                    
                    <dxlc:LayoutItem DockPanel.Dock="Right" HorizontalAlignment="Right">
                        <Button 
                    		Command="{Binding SearchRightCommand }"
                    		Content="{Binding SearchRightContent, FallbackValue='SearchRight' }"
                    		ToolTip="{Binding SearchRightToolTip, FallbackValue='SearchRight ToolTip' }" />
                    </dxlc:LayoutItem>
                    <!-- <dxlc:LayoutItem> -->
                    <!-- <Button -->
                    <!-- Command="{Binding SearchRightCommand}" -->
                    <!-- CommandParameter="{Binding SearchRightCommandParameter}" -->
                    <!-- Content="{Binding SearchRightContent, FallbackValue='SearchRight' }" -->
                    <!-- ToolTip="{Binding SearchRightToolTip, FallbackValue='SearchRight ToolTip' }" /> -->
                    <!-- <Button.Background> -->
                    <!-- <ImageBrush ImageSource="\Resources\Images\VNCDeveloperMotivation.png"  /> -->
                    <!-- </Button.Background> -->
                    <!-- </dxlc:LayoutItem> -->
                    <!-- Second form uses CommandParameter -->
                    




                </DockPanel>

            </dxlc:LayoutGroup>

        </DockPanel>

    </DockPanel>

</vncmvvm:ViewBase>
