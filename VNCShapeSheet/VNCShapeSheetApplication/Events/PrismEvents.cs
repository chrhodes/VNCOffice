using Prism.Events;

namespace VNCShapeSheetApplication.Core
{
    public class SelectionChangedEvent : PubSubEvent
    {
    }

    // TODO(crhodes)
    // If a VNCShapeSheetApplication.Core assembly is created,
    // move this event and any other Prism events there
}
